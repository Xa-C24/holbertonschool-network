#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine it is executed on.

# Extract and list only IPv4 addresses in a consistent format.
ip -4 -o addr show | awk '{print $4}' | cut -d'/' -f1 | sort -u
